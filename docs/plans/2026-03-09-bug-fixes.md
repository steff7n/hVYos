# Multi-Bug Fixes Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Fix the bug-hunt findings in `BUG_HUNT_REPORT.md` with runtime evidence, failing tests first, and post-fix verification.

**Architecture:** Work in small slices by subsystem: reproduce with minimal instrumentation, add a focused failing regression test, implement the smallest production change, then rerun targeted verification and the repo test suite. Keep instrumentation in place until post-fix verification is complete.

**Tech Stack:** Python 3, `pytest`/`unittest`, shell scripts, repo-local debug NDJSON logging

---

### Task 1: Confirm candidate bugs with instrumentation

**Files:**
- Modify: `packages/linta-welcome/linta_welcome.py`
- Modify: `packages/linta-keybindings/linta_keybindings.py`
- Modify: `scripts/build-with-container.sh`

**Step 1: Add minimal debug logging**

- Log marker path/write attempt in `linta_welcome.py`.
- Log primary screen presence in `linta_keybindings.py`.
- Log runtime/image-check branch in `build-with-container.sh`.

**Step 2: Clear debug log and run targeted reproductions**

Run small harnesses that trigger:
- welcome marker write failure
- keybindings with no primary screen
- container script with Docker-compatible fake runtime behavior

**Step 3: Analyze the logs**

Expected:
- clear proof whether each candidate is real
- reject any candidate that does not reproduce

### Task 2: Add failing tests for confirmed `lintactl` bugs

**Files:**
- Modify: `packages/lintactl/test_lintactl.py`
- Modify: `packages/lintactl/lintactl.py`

**Step 1: Write failing tests**

- test that `theme set ../evil` is rejected
- test that `info` prints the actual filesystem label for non-Btrfs roots

**Step 2: Run tests to verify they fail**

Run: `cd packages/lintactl && python3 -m pytest test_lintactl.py -v`

**Step 3: Implement minimal fix**

- validate theme name/path containment
- separate filesystem type reporting from Btrfs status wording

**Step 4: Rerun tests**

Expected: target tests pass and no regressions in `test_lintactl.py`

### Task 3: Add failing tests for confirmed `linta-snapshots` bug

**Files:**
- Modify: `packages/linta-snapshots/test_linta_snapshots.py`
- Modify: `packages/linta-snapshots/linta_snapshots.py`

**Step 1: Write failing test**

- generated GRUB script should embed a concrete Btrfs UUID instead of `$btrfs_uuid`

**Step 2: Run test to verify it fails**

Run: `cd packages/linta-snapshots && python3 -m pytest test_linta_snapshots.py -v`

**Step 3: Implement minimal fix**

- resolve the root Btrfs UUID before generating entries
- fail safely when UUID cannot be determined

**Step 4: Rerun tests**

Expected: target tests pass and no regressions in `test_linta_snapshots.py`

### Task 4: Add failing tests for confirmed `linta-nvidia` bug

**Files:**
- Modify: `packages/linta-nvidia/test_linta_nvidia.py`
- Modify: `packages/linta-nvidia/linta_nvidia.py`

**Step 1: Write failing test**

- `_enable_rpm_fusion()` should use expanded release URLs on its first install attempt

**Step 2: Run test to verify it fails**

Run: `cd packages/linta-nvidia && python3 -m pytest test_linta_nvidia.py -v`

**Step 3: Implement minimal fix**

- compute Fedora version once up front
- use a single deterministic install command

**Step 4: Rerun tests**

Expected: target tests pass and no regressions in `test_linta_nvidia.py`

### Task 5: Add failing tests for installer NVMe bug

**Files:**
- Create: `installer/test_linta_installer.py`
- Modify: `installer/linta_installer.py`

**Step 1: Write failing test**

- NVMe disk `/dev/nvme0n1` should produce `p1/p2/p3` partition paths

**Step 2: Run test to verify it fails**

Run: `cd installer && python3 -m pytest test_linta_installer.py -v`

**Step 3: Implement minimal fix**

- extract partition path helper and replace all raw `f"{disk}N"` usages

**Step 4: Rerun tests**

Expected: installer test passes

### Task 6: Add failing tests for newly confirmed candidate bugs

**Files:**
- Create or modify targeted tests near each affected package/script

**Step 1: Write failing tests**

- welcome: finish flow should not crash when marker write is denied
- keybindings: no primary screen should not raise `AttributeError`
- build script: Docker path should use a Docker-supported image-existence check

**Step 2: Run each test/harness to verify failure**

**Step 3: Implement minimal fixes**

- handle marker write failure explicitly
- guard missing primary screen
- use runtime-specific image existence probing

**Step 4: Rerun targeted verification**

Expected: all candidate-bug regressions now pass

### Task 7: Full verification

**Files:**
- Verify only

**Step 1: Clear debug log**

**Step 2: Run targeted package tests and harnesses**

Run:
- `make test`
- any new targeted tests/harnesses for installer, welcome, keybindings, and scripts

**Step 3: Read debug log and confirm post-fix behavior**

Expected:
- hypotheses resolved with runtime evidence
- no previously failing reproduction still occurs
