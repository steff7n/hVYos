# Linta Linux — Package cache pruning timer
# Profiles: [All]
# Spec: README.md §3.9 — weekly, remove cached packages older than 30 days

[Unit]
Description=Weekly package cache pruning

[Timer]
OnCalendar=weekly
RandomizedDelaySec=6h
Persistent=true

[Install]
WantedBy=timers.target
